import React from 'react'

const SectionHeading = ({title}) => {
  return (
      <h1 className="text-center text-4xl tracking-[0.3em] bg-white/40 py-3 rounded-2xl flex justify-center uppercase font-extralight">{title}</h1>
  )
}

export default SectionHeading